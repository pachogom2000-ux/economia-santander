<!doctype html>
<html lang="es">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
{% set pageTitle = title or "Economía Santander | Noticias de economía en Santander" %}
{% set pageDescription = description or "Noticias económicas de Santander, Colombia: inversión, empleo, construcción, agro e indicadores. Análisis y reportería con más de 20 años de experiencia." %}
<title>{{ pageTitle }}</title>
<meta name="description" content="{{ pageDescription }}">
<link rel="canonical" href="https://economiasantander.com{{ page.url }}">
<meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1">
<meta name="author" content="{{ autor or 'Economía Santander' }}">
<meta name="theme-color" content="#0B6E3B" media="(prefers-color-scheme: light)">
<meta name="theme-color" content="#0C130E" media="(prefers-color-scheme: dark)">
<link rel="stylesheet" href="/assets/style.css">
<!-- Open Graph -->
<meta property="og:type" content="{{ ogType or 'website' }}">
<meta property="og:site_name" content="Economía Santander">
<meta property="og:title" content="{{ pageTitle }}">
<meta property="og:description" content="{{ pageDescription }}">
<meta property="og:url" content="https://economiasantander.com{{ page.url }}">
<meta property="og:locale" content="es_CO">
{% set ogImagen = imagen or "/assets/uploads/empleo-formal-bucaramanga-area-metropolitana-2026.jpg" %}
<meta property="og:image" content="https://economiasantander.com{{ ogImagen }}">
{% if imagenAlt %}<meta property="og:image:alt" content="{{ imagenAlt }}">{% endif %}
<!-- Twitter Cards -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="{{ pageTitle }}">
<meta name="twitter:description" content="{{ pageDescription }}">
<meta name="twitter:image" content="https://economiasantander.com{{ ogImagen }}">
{% if imagen %}<link rel="preload" as="image" href="{{ imagen }}" fetchpriority="high">{% endif %}
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-HVC4Z011SY"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-HVC4Z011SY');
</script>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebSite",
      "@id": "https://economiasantander.com/#website",
      "url": "https://economiasantander.com/",
      "name": "Economía Santander",
      "description": "Portal de noticias económicas del departamento de Santander, Colombia.",
      "inLanguage": "es-CO",
      "publisher": { "@id": "https://economiasantander.com/#organization" },
      "potentialAction": {
        "@type": "SearchAction",
        "target": {
          "@type": "EntryPoint",
          "urlTemplate": "https://economiasantander.com/?s={search_term_string}"
        },
        "query-input": "required name=search_term_string"
      }
    },
    {
      "@type": "NewsMediaOrganization",
      "@id": "https://economiasantander.com/#organization",
      "name": "Economía Santander",
      "url": "https://economiasantander.com/",
      "description": "Medio nativo digital especializado en la economía del departamento de Santander, Colombia.",
      "foundingDate": "2026",
      "areaServed": {
        "@type": "AdministrativeArea",
        "name": "Santander, Colombia"
      },
      "knowsAbout": ["Economía regional", "Inversión", "Empleo", "Construcción", "Industria", "Agroindustria", "Finanzas personales"],
      "founder": { "@id": "https://economiasantander.com/#periodista" }
    },
    {
      "@type": "Person",
      "@id": "https://economiasantander.com/#periodista",
      "name": "NOMBRE DEL PERIODISTA (reemplazar)",
      "jobTitle": "Periodista económico",
      "description": "Periodista económico con más de 20 años cubriendo la economía del oriente colombiano.",
      "url": "https://economiasantander.com/autor/",
      "worksFor": { "@id": "https://economiasantander.com/#organization" },
      "sameAs": [
        "https://www.linkedin.com/in/USUARIO-LINKEDIN-reemplazar"
      ]
    }
  ]
}
</script>

</head>
<body>


<div class="pico-ribbon">
  <div class="wrap pico-inner">
    <span class="pico-title">Pico y placa hoy</span>
    <div class="pico-cities" id="pico-cities">
      <span class="pico-city"><b>Bucaramanga</b> <span class="pico-digits">—</span></span>
      <span class="pico-city"><b>Bogotá</b> <span class="pico-digits">—</span></span>
      <span class="pico-city"><b>Medellín</b> <span class="pico-digits">—</span></span>
    </div>
  </div>
</div>

<div class="utility-bar">
  <div class="wrap utility-inner">
    <span class="utility-date" id="fecha-hoy">Bucaramanga, Colombia · julio de 2026</span>
    <span class="ticker-label">Mercados <span class="live-badge" id="trm-live"><span class="dot"></span>EN VIVO</span></span>
  </div>
  <div class="ticker-viewport">
    <div class="ticker-track" id="ticker-track">
      <div class="ticker-set">
        <span class="ticker-item live js-usd-item" title="Último cierre del dólar spot en Colombia (SetFX), obtenido automáticamente vía dolarapi.com. Se actualiza en cada visita."><span class="name">USD/COP</span><span class="val js-usd-val">$3.214,40</span><span class="delta flat js-usd-delta">cierre</span></span>
        <span class="ticker-item live" title="Oro (aprox.) — referencia en vivo vía PAXG/CoinGecko, token respaldado 1:1 por una onza troy de oro (USD/oz)."><span class="name">Oro</span><span class="val js-oro-val">US$4.050</span><span class="delta flat js-oro-delta">≈ oz</span></span>
        <span class="ticker-item live" title="Bitcoin (BTC/USD). Fuente: CoinGecko, en vivo con variación de 24 h."><span class="name">Bitcoin</span><span class="val js-btc-val">US$64.300</span><span class="delta flat js-btc-delta">cripto</span></span>
        <span class="ticker-item" title="Ecopetrol (BVC). Cierre del 24 de julio de 2026."><span class="name">Ecopetrol</span><span class="val">$2.590</span><span class="delta down">▼ 0,58%</span></span>
        <span class="ticker-item" title="Bancolombia, que cotiza bajo su holding Grupo Cibest (BVC). Cierre del 24 de julio de 2026."><span class="name">Bancolombia (Cibest)</span><span class="val">$81.020</span><span class="delta down">▼ 2,53%</span></span>
        <span class="ticker-item" title="Grupo Sura (BVC). Cierre del 24 de julio de 2026."><span class="name">Grupo Sura</span><span class="val">$53.540</span><span class="delta up">▲ 1,02%</span></span>
        <span class="ticker-item" title="Interconexión Eléctrica S.A. — ISA (BVC). Cierre del 24 de julio de 2026."><span class="name">ISA</span><span class="val">$28.740</span><span class="delta down">▼ 1,58%</span></span>
        <span class="ticker-item" title="Grupo Argos (BVC). Cierre del 24 de julio de 2026."><span class="name">Grupo Argos</span><span class="val">$16.300</span><span class="delta up">▲ 0,87%</span></span>
        <span class="ticker-item" title="Tecnoglass (NYSE, en dólares). Cierre del 24 de julio de 2026."><span class="name">Tecnoglass</span><span class="val">US$45,30</span><span class="delta up">▲ 3,57%</span></span>
        <span class="ticker-item" title="Grupo Nutresa (BVC). Cierre del 24 de julio de 2026."><span class="name">Nutresa</span><span class="val">$312.000</span><span class="delta down">▼ 0,10%</span></span>
        <span class="ticker-item" title="Banco de Bogotá (BVC). Cierre del 24 de julio de 2026."><span class="name">Banco de Bogotá</span><span class="val">$37.100</span><span class="delta down">▼ 0,91%</span></span>
        <span class="ticker-item" title="Davivienda — acción preferencial (BVC). Cierre del 24 de julio de 2026."><span class="name">Davivienda</span><span class="val">$24.180</span><span class="delta up">▲ 0,67%</span></span>
        <span class="ticker-item" title="Celsia (BVC), sector energía. Sustituye a Isagén, que ya no cotiza en la Bolsa de Valores de Colombia desde 2017. Cierre del 24 de julio de 2026."><span class="name">Celsia</span><span class="val">$4.950</span><span class="delta down">▼ 2,94%</span></span>
        <span class="ticker-item" title="Índice MSCI COLCAP de la Bolsa de Valores de Colombia (puntos). Cierre del 25 de julio de 2026."><span class="name">COLCAP</span><span class="val">2.274,53</span><span class="delta down">▼ 0,38%</span></span>
        <span class="ticker-item" title="Rendimiento de los TES a 10 años (deuda pública colombiana). Referencia de julio de 2026."><span class="name">TES 10 años</span><span class="val">13,20%</span><span class="delta flat">jul</span></span>
        <span class="ticker-item" title="Tasa de usura para crédito de consumo y ordinario, certificada por la Superintendencia Financiera para julio de 2026. Es el interés máximo legal."><span class="name">Tasa de usura</span><span class="val">28,79%</span><span class="delta flat">tope legal</span></span>
        <span class="ticker-item" title="Precio interno de referencia de la carga de 125 kg de café pergamino seco. Fuente: Federación Nacional de Cafeteros, 24 de julio de 2026."><span class="name">Café (carga)</span><span class="val">$2.160.000</span><span class="delta flat">24 jul</span></span>
        <span class="ticker-item" title="Petróleo Brent, referencia internacional (USD/barril). Cierre del 24 de julio de 2026."><span class="name">Brent</span><span class="val">US$98,31</span><span class="delta flat">24 jul</span></span>
        <span class="ticker-item" title="Índice S&amp;P 500 (EE. UU.), en puntos. Referencia del 16 de julio de 2026."><span class="name">S&amp;P 500</span><span class="val">7.533,77</span><span class="delta flat">16 jul</span></span>
        <span class="ticker-item" title="Valor aproximado de la UVR al 25 de julio de 2026. Fuente: Banco de la República."><span class="name">UVR</span><span class="val">$416,19</span><span class="delta flat">25 jul</span></span>
        <span class="ticker-item" title="IPC — variación anual de junio de 2026. Fuente: DANE."><span class="name">Inflación anual</span><span class="val">6,14%</span><span class="delta up">▲ 0,30 pp</span></span>
        <span class="ticker-item" title="Tasa de intervención de política monetaria, vigente desde el 3 de julio de 2026. Fuente: Banco de la República."><span class="name">Tasa BanRep</span><span class="val">12,00%</span><span class="delta up">▲ 0,75 pp</span></span>
      </div>
    </div>
  </div>
</div>

<header class="masthead">
  <div class="wrap">
    <p class="kicker">Periodismo económico regional</p>
    {# El H1 debe ser único por página: en la portada lo es el nombre del medio;
       en una noticia, el H1 es el titular, así que aquí baja a <p>. #}
    {% if page.url == "/" %}
    <h1><a href="/" style="color:inherit; text-decoration:none;">Economía <span class="leo">Santander</span></a></h1>
    {% else %}
    <p class="marca"><a href="/" style="color:inherit; text-decoration:none;">Economía <span class="leo">Santander</span></a></p>
    {% endif %}
    <p class="tagline">Inversión, empleo y desarrollo del oriente colombiano, con veinte años de reportería económica detrás de cada historia.</p>
  </div>
</header>

<nav class="primary">
  <div class="wrap">
    <button type="button" class="nav-toggle" id="nav-toggle" aria-expanded="false" aria-controls="nav-menu">
      <span class="nav-burger" aria-hidden="true"></span>
      Secciones
      <span class="nav-toggle-hint">Ver todas</span>
    </button>
    <div class="nav-inner" id="nav-menu">
    <a href="/" class="current">Portada</a>
    <a href="#">Inversión</a>
    <a href="#">Finanzas personales</a>
    <a href="#">Tasas</a>
    <a href="#">Empleo</a>
    <a href="#">Construcción</a>
    <a href="#">Industria</a>
    <a href="#">Agro</a>
    <a href="#">Educación financiera</a>
    <a href="#">Opinión</a>
    <a href="#">Entrevistas</a>
    <a href="#">Glosario</a>
    </div>
  </div>
</nav>

<!-- Anuncio vendido: Tienda Sinestrés (billboard superior) -->
<div class="wrap ad-wrap">
  <div class="ad-unit ad-cafe">
    <span class="ad-tag">Publicidad</span>
    <div class="ad-media" aria-hidden="true">
      <img src="/assets/ad-tinto.jpg" alt="" loading="lazy" decoding="async" width="1024" height="683">
    </div>
    <div class="ad-body">
      <div class="ad-copy">
        <p class="ad-brand">Tienda Sinestrés · Bucaramanga</p>
        <h3 class="ad-headline">Tu tinto va por la casa</h3>
        <p class="ad-text">Genera tu cupón, muéstralo en caja y reclama un tinto gratis. <strong>Solo 100 cupones</strong>, uno por dispositivo.</p>
        <div class="ad-actions">
          <button type="button" class="ad-cta primary" id="btn-cupon">Generar mi cupón</button>
          <a class="ad-cta ghost" id="link-sinestres" href="https://maps.app.goo.gl/8Lopf2Nfjzwn2ymj7" target="_blank" rel="noopener sponsored">Cómo llegar →</a>
        </div>
      </div>
      <div class="ad-coupon" id="cupon-tinto" hidden>
        <img id="qr-tinto" width="108" height="108" alt="Código QR que abre la ubicación de Tienda Sinestrés en Google Maps"
             src="data:image/png;base64,">
        <div>
          <p class="coupon-title">Cupón · Tinto gratis</p>
          <p class="coupon-code" id="cupon-codigo">TINTO-0000</p>
          <p class="coupon-valid" id="cupon-vigencia">Escanea el QR para llegar a la tienda.</p>
        </div>
      </div>
    </div>
  </div>
</div>

<main>
{{ content | safe }}
</main>

<footer>
  <div class="wrap">
    <div class="footer-grid">
      <div class="author-block">
        <h3>[Nombre del periodista]</h3>
        <p>Periodista económico con más de 20 años cubriendo la economía del oriente colombiano — trayectoria, medios previos y reconocimientos van aquí.</p>
        <div class="author-links">
          <a href="#">LinkedIn ↗</a>
          <a href="#">Publicaciones anteriores ↗</a>
        </div>
      </div>
      <div class="footer-col">
        <h4>Secciones</h4>
        <ul>
          <li><a href="#">Inversión</a></li>
          <li><a href="#">Empleo</a></li>
          <li><a href="#">Construcción</a></li>
          <li><a href="#">Agro</a></li>
        </ul>
      </div>
      <div class="footer-col">
        <h4>Sobre el medio</h4>
        <ul>
          <li><a href="#">Cómo trabajamos</a></li>
          <li><a href="#">Política editorial</a></li>
          <li><a href="#">Contacto</a></li>
        </ul>
      </div>
      <div class="footer-col">
        <h4>Síguenos</h4>
        <ul>
          <li><a href="#">LinkedIn</a></li>
          <li><a href="#">Instagram</a></li>
          <li><a href="#">Boletín por correo</a></li>
        </ul>
      </div>
    </div>
  </div>
</footer>

<script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
<script>
  if (window.netlifyIdentity) {
    window.netlifyIdentity.on("init", user => {});
    window.netlifyIdentity.on("login", () => {
      document.location.href = "/admin/";
    });
  }
</script>

<script>
  (function () {
    // ---------- utilidades ----------
    function each(sel, fn) {
      var nodes = document.querySelectorAll(sel);
      for (var i = 0; i < nodes.length; i++) { fn(nodes[i]); }
    }
    function fmtNum(n, dec) {
      return n.toLocaleString('es-CO', { minimumFractionDigits: dec, maximumFractionDigits: dec });
    }
    // Fecha de hoy en la zona horaria de Colombia, como objeto Date local
    function hoyBogota() {
      try {
        var p = new Intl.DateTimeFormat('en-US', {
          timeZone: 'America/Bogota', year: 'numeric', month: '2-digit', day: '2-digit'
        }).formatToParts(new Date());
        var o = {};
        for (var i = 0; i < p.length; i++) { o[p[i].type] = p[i].value; }
        return new Date(Number(o.year), Number(o.month) - 1, Number(o.day));
      } catch (e) { return new Date(); }
    }

    // ---------- 1) Fecha del encabezado ----------
    try {
      var hoyTxt = new Intl.DateTimeFormat('es-CO', {
        weekday: 'long', day: 'numeric', month: 'long', year: 'numeric', timeZone: 'America/Bogota'
      }).format(new Date());
      hoyTxt = hoyTxt.charAt(0).toUpperCase() + hoyTxt.slice(1);
      var elFecha = document.getElementById('fecha-hoy');
      if (elFecha) elFecha.textContent = 'Bucaramanga, Colombia · ' + hoyTxt;
    } catch (e) {}

    // ---------- 2) Pico y placa ----------
    (function () {
      var cont = document.getElementById('pico-cities');
      if (!cont) return;
      var d = hoyBogota();
      var dow = d.getDay();            // 0 domingo … 6 sábado
      var dia = d.getDate();

      var bga = { 1: '7 y 8', 2: '9 y 0', 3: '1 y 2', 4: '3 y 4', 5: '5 y 6' };
      var med = { 1: '5 y 8', 2: '1 y 4', 3: '0 y 2', 4: '3 y 6', 5: '7 y 9' };

      // Bucaramanga: sábados con rotación propia (ref.: sábado 25 jul 2026 = "7 y 8")
      function sabadoBga() {
        var ciclo = ['1 y 2', '3 y 4', '5 y 6', '7 y 8', '9 y 0'];
        var ref = new Date(2026, 6, 25);
        var semanas = Math.round((d - ref) / 604800000);
        return ciclo[((3 + semanas) % 5 + 5) % 5];
      }

      var datos = [];
      if (dow >= 1 && dow <= 5) {
        datos.push({ c: 'Bucaramanga', v: bga[dow], h: '6:00 a 20:00' });
        datos.push({ c: 'Bogotá', v: (dia % 2 === 1) ? '6, 7, 8, 9 y 0' : '1, 2, 3, 4 y 5', h: '6:00 a 21:00' });
        datos.push({ c: 'Medellín', v: med[dow], h: '5:00 a 20:00' });
      } else if (dow === 6) {
        datos.push({ c: 'Bucaramanga', v: sabadoBga(), h: '9:00 a 13:00' });
        datos.push({ c: 'Bogotá', v: null, h: 'sin restricción' });
        datos.push({ c: 'Medellín', v: null, h: 'sin restricción' });
      } else {
        datos.push({ c: 'Bucaramanga', v: null, h: 'sin restricción' });
        datos.push({ c: 'Bogotá', v: null, h: 'sin restricción' });
        datos.push({ c: 'Medellín', v: null, h: 'sin restricción' });
      }

      cont.textContent = '';
      for (var i = 0; i < datos.length; i++) {
        var it = datos[i];
        var wrap = document.createElement('span');
        wrap.className = 'pico-city';
        var b = document.createElement('b');
        b.textContent = it.c;
        var dig = document.createElement('span');
        dig.className = 'pico-digits' + (it.v ? '' : ' libre');
        dig.textContent = it.v ? it.v : 'Libre';
        var tm = document.createElement('span');
        tm.className = 'pico-time';
        tm.textContent = it.h;
        wrap.appendChild(b);
        wrap.appendChild(dig);
        wrap.appendChild(tm);
        wrap.title = it.v
          ? 'Placas restringidas hoy en ' + it.c + ' para vehículos particulares, ' + it.h + '.'
          : 'Hoy no hay pico y placa para vehículos particulares en ' + it.c + '.';
        cont.appendChild(wrap);
      }
    })();

    // ---------- 3) Cinta bursátil: duplicar para desplazamiento continuo ----------
    (function () {
      var track = document.getElementById('ticker-track');
      if (!track) return;
      var set = track.querySelector('.ticker-set');
      if (!set) return;
      var clon = set.cloneNode(true);
      clon.setAttribute('aria-hidden', 'true');
      track.appendChild(clon);
    })();

    var elLive = document.getElementById('trm-live');

    // ---------- 4) USD/COP: último cierre del dólar spot (dolarapi.com) ----------
    fetch('https://co.dolarapi.com/v1/cotizaciones')
      .then(function (r) { return r.ok ? r.json() : Promise.reject(); })
      .then(function (list) {
        var usd = null;
        for (var i = 0; i < list.length; i++) { if (list[i].moneda === 'USD') { usd = list[i]; break; } }
        if (!usd) return;
        var cierre = parseFloat(usd.ultimoCierre);
        if (!isNaN(cierre)) each('.js-usd-val', function (el) { el.textContent = '$' + fmtNum(cierre, 2); });
        try {
          var ff = new Intl.DateTimeFormat('es-CO', { day: 'numeric', month: 'short', timeZone: 'America/Bogota' })
            .format(new Date(usd.fechaActualizacion));
          each('.js-usd-delta', function (el) { el.textContent = 'cierre · ' + ff; });
        } catch (e) {}
        var c = parseFloat(usd.compra), v = parseFloat(usd.venta);
        each('.js-usd-item', function (el) {
          el.title = 'Último cierre del dólar spot en Colombia (SetFX), vía dolarapi.com — se actualiza en cada visita.'
            + (!isNaN(c) ? ' · Compra $' + fmtNum(c, 2) : '')
            + (!isNaN(v) ? ' · Venta $' + fmtNum(v, 2) : '');
        });
        if (elLive) elLive.classList.add('on');
      })
      .catch(function () {});

    // ---------- 5) Oro y Bitcoin en vivo (CoinGecko) ----------
    function setCripto(clave, precio, cambio) {
      if (!isNaN(precio)) {
        each('.js-' + clave + '-val', function (el) { el.textContent = 'US$' + Math.round(precio).toLocaleString('es-CO'); });
      }
      if (cambio != null && !isNaN(cambio)) {
        var sube = cambio >= 0;
        each('.js-' + clave + '-delta', function (el) {
          el.textContent = (sube ? '▲ ' : '▼ ') + fmtNum(Math.abs(cambio), 2) + '%';
          el.className = 'delta js-' + clave + '-delta ' + (sube ? 'up' : 'down');
        });
      }
    }
    fetch('https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,pax-gold&vs_currencies=usd&include_24hr_change=true')
      .then(function (r) { return r.ok ? r.json() : Promise.reject(); })
      .then(function (j) {
        if (j.bitcoin) setCripto('btc', parseFloat(j.bitcoin.usd), parseFloat(j.bitcoin.usd_24h_change));
        if (j['pax-gold']) setCripto('oro', parseFloat(j['pax-gold'].usd), parseFloat(j['pax-gold'].usd_24h_change));
        if (elLive) elLive.classList.add('on');
      })
      .catch(function () {});

    // ---------- 6) Menú de secciones en móvil ----------
    (function () {
      var btn = document.getElementById('nav-toggle');
      var menu = document.getElementById('nav-menu');
      if (!btn || !menu) return;
      btn.addEventListener('click', function () {
        var abierto = menu.classList.toggle('is-open');
        btn.setAttribute('aria-expanded', abierto ? 'true' : 'false');
        var hint = btn.querySelector('.nav-toggle-hint');
        if (hint) hint.textContent = abierto ? 'Cerrar' : 'Ver todas';
      });
      // Al elegir una sección, cerrar el menú
      each('#nav-menu a', function (a) {
        a.addEventListener('click', function () {
          if (menu.classList.contains('is-open')) {
            menu.classList.remove('is-open');
            btn.setAttribute('aria-expanded', 'false');
            var h = btn.querySelector('.nav-toggle-hint');
            if (h) h.textContent = 'Ver todas';
          }
        });
      });
    })();

    // ---------- 7) Cupón del anunciante: uno por dispositivo, 100 disponibles ----------
    (function () {
      var btn = document.getElementById('btn-cupon');
      var caja = document.getElementById('cupon-tinto');
      if (!btn || !caja) return;

      var LLAVE = 'sinestres-tinto-2026';
      var TOTAL = 100;

      function guardado() {
        try { return JSON.parse(window.localStorage.getItem(LLAVE) || 'null'); }
        catch (e) { return null; }
      }
      function guardar(dato) {
        try { window.localStorage.setItem(LLAVE, JSON.stringify(dato)); } catch (e) {}
      }
      function numeroAleatorio() {
        if (window.crypto && window.crypto.getRandomValues) {
          var a = new Uint32Array(1);
          window.crypto.getRandomValues(a);
          return (a[0] % TOTAL) + 1;
        }
        return Math.floor(Math.random() * TOTAL) + 1;
      }
      function fechaLarga(iso) {
        try {
          return new Intl.DateTimeFormat('es-CO', {
            day: 'numeric', month: 'long', year: 'numeric', timeZone: 'America/Bogota'
          }).format(iso ? new Date(iso) : new Date());
        } catch (e) { return ''; }
      }
      function pintar(dato) {
        var elCod = document.getElementById('cupon-codigo');
        var elVig = document.getElementById('cupon-vigencia');
        if (elCod) elCod.textContent = 'TINTO-' + ('00' + dato.n).slice(-3);
        if (elVig) {
          elVig.textContent = 'Cupón ' + dato.n + ' de ' + TOTAL + '. Emitido el ' + fechaLarga(dato.fecha)
            + '. Uno por dispositivo: muéstralo en caja y escanea el QR para llegar a la tienda.';
        }
        caja.hidden = false;
        btn.textContent = 'Ver mi cupón';
        btn.setAttribute('aria-disabled', 'true');
      }

      var previo = guardado();
      if (previo && previo.n) { pintar(previo); }

      btn.addEventListener('click', function () {
        var actual = guardado();
        if (!actual || !actual.n) {
          actual = { n: numeroAleatorio(), fecha: new Date().toISOString() };
          guardar(actual);
        }
        pintar(actual);
        caja.setAttribute('tabindex', '-1');
        caja.focus();
      });
    })();

    // ---------- 8) Pestañas de tasas ----------
    (function () {
      var tabs = document.querySelectorAll('.tab-btn');
      if (!tabs.length) return;
      function activar(tab) {
        for (var i = 0; i < tabs.length; i++) {
          var t = tabs[i];
          var sel = (t === tab);
          t.setAttribute('aria-selected', sel ? 'true' : 'false');
          t.tabIndex = sel ? 0 : -1;
          var panel = document.getElementById(t.getAttribute('aria-controls'));
          if (panel) panel.hidden = !sel;
        }
      }
      for (var i = 0; i < tabs.length; i++) {
        tabs[i].addEventListener('click', function () { activar(this); });
        tabs[i].addEventListener('keydown', function (e) {
          var lista = Array.prototype.slice.call(tabs);
          var pos = lista.indexOf(this);
          var sig = null;
          if (e.key === 'ArrowRight') { sig = lista[(pos + 1) % lista.length]; }
          if (e.key === 'ArrowLeft') { sig = lista[(pos - 1 + lista.length) % lista.length]; }
          if (sig) { e.preventDefault(); activar(sig); sig.focus(); }
        });
      }
    })();

    // ---------- 9) Tarjetas flip ----------
    each('.flip-btn', function (btn) {
      btn.addEventListener('click', function () {
        var card = btn.closest('.flip-card');
        if (!card) return;
        var abierta = card.classList.toggle('is-flipped');
        var frente = card.querySelector('.flip-front .flip-btn');
        var reverso = card.querySelector('.flip-back .flip-btn');
        if (frente) frente.setAttribute('aria-expanded', abierta ? 'true' : 'false');
        if (reverso) reverso.setAttribute('aria-expanded', abierta ? 'true' : 'false');
        var destino = abierta ? reverso : frente;
        if (destino) { window.setTimeout(function () { destino.focus(); }, 320); }
      });
    });
  })();
</script>

</body>
</html>
